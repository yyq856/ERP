<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="webserver.mapper.BusinessPartnerMapper">

    <!-- BP维护页面 - 插入新的业务伙伴（简化版） -->
    <insert id="insertBusinessPartnerForMaintain" 
            parameterType="webserver.pojo.BusinessPartner"
            useGeneratedKeys="true" 
            keyProperty="customerId" 
            keyColumn="customer_id">
        INSERT INTO erp_customer (
            title, name, first_name, last_name, bp_type, search_term,
            language, street, city, region, postal_code, country, 
            company_code, reconciliation_account, sort_key, sales_org, 
            channel, division, currency, sales_district, price_group,
            customer_group, delivery_priority, shipping_condition, 
            delivering_plant, max_part_deliv, incoterms, incoterms_location, 
            payment_terms, acct_assignment, output_tax
        ) VALUES (
            #{title}, #{name}, #{firstName}, #{lastName}, #{bpType}, #{searchTerm},
            'EN', #{street}, #{city}, 'CN', #{postalCode}, #{country},
            '1000', '140000', '001', '1000', 10, '01', 'CNY', '000001', '01',
            '01', '01', '01', 1000, 1, 'EXW', 'Shanghai', '0001', '01', 1
        )
    </insert>

</mapper>
